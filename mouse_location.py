import pyautogui as bot
import time
import gui_tools as gt
from collections import namedtuple

# resolution_location = namedtuple(['resolution, x_loc, y_loc'])

# x, y = bot.size()
# print(str(x) + ", " + str(y))


time.sleep(2)
# gt.screencapt('img/contact_record_create/new_contact.png')
gt.track_mouse()

